The directories `input` and `output` are the embedded flexible runs.

The directories `input_2` and `output_2` are the embedded flexible runs after restart from the flexible frozen, from 2.5 seconds.

Energy conservation issues are experienced.
